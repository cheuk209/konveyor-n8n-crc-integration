apiVersion: v1
kind: Service
metadata:
  name: k8smcp
  namespace: k8smcp
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: mcpk8s
  type: ClusterIP
